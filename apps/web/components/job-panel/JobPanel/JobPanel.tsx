import { Card } from "../../ui/Card/Card";

export function JobPanel() {
  return (
    <aside style={{ borderLeft: "1px solid var(--color-border)", padding: "var(--space-lg)" }}>
      <Card>
        <h2>Job panel shell</h2>
        <p>Match breakdown and quick prep components land here in later steps.</p>
      </Card>
    </aside>
  );
}
