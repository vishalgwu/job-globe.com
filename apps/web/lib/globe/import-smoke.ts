import { Deck } from "@deck.gl/core";
import { Canvas } from "@react-three/fiber";
import createGlobe from "globe.gl";
import { Scene } from "three";

export const globeDependencySmoke = {
  Deck,
  Canvas,
  createGlobe,
  Scene
};
