# Accessibility Notes

Purpose: reserved location for shared accessibility helpers.

Current status: accessibility behavior is implemented mostly inside components, including reduced-motion checks, fallback/list modes, focusable controls, labels, and standard HTML controls.

Launch QA still needs recorded keyboard, VoiceOver, and NVDA checks. See `docs/md/PROJECT_STATUS.md`.
